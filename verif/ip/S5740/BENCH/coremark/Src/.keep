main() lives in Middleware/benchmark/coremark/core_main.c (upstream, unmodified),
and the porting layer is Middleware/benchmark/coremark/port/core_portme.c.
So it is normal for this Src/ directory to be empty (the
FW_SRC = wildcard $(verif_dir)/Src/*.c in Makefile.VERIF yields an empty list).
