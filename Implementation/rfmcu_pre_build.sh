# PLACEHOLDER - paste the original Implementation/rfmcu_pre_build.sh, with:
#   ${LOCL_HEADER_FILE}  ->  ${LOCAL_HEADER_FILE}        (LOCL typo)
#   S5651_HEAER_PATH / S5651F_HEAER_PATH -> *_HEADER_PATH (HEAER typo)
#   verify whether ${RFMCU_HEADER_FILE} is used without definition (appears undefined)
#   CRLF -> LF.
